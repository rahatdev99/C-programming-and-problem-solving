#include <stdio.h>

int main()
{
    int n,a,b,t;
    scanf("%d",&n);

    while(n--) {
        scanf("%d",&t);
        
        a = t / 4;

        if(t <= 20) {
            
        }
        
        printf("%d %d\n",a,b);
    }
    return 0;
}